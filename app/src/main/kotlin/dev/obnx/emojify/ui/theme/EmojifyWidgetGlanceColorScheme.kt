package dev.obnx.emojify.ui.theme

import androidx.glance.material3.ColorProviders

object EmojifyWidgetGlanceColorScheme {
    val colors = ColorProviders(
        light = lightScheme,
        dark = darkScheme,
    )
}